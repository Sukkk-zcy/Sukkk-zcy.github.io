---
title: "P4 程序的骨架：从输入到输出"
publishDate: 2026-07-21 00:00:00
description: "理解 P4 程序的整体结构和数据流向"
tags: ["P4", "架构", "入门"]
categories: ["P4"]
language: "中文"
draft: false
comment: true
---

上一篇了解了 P4 的背景，这一篇来认识 P4 程序的整体结构——就像盖房子先看图纸一样。

---

## 一、P4 程序处理报文的流程

一个数据包进入交换机后，会经过以下几个阶段：

<div style="background: #0a0a0a; border-radius: 12px; padding: 20px; margin: 20px 0;">
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 400" style="width: 100%; height: auto;">
  <!-- 标题 -->
  <text x="500" y="40" text-anchor="middle" fill="#ffffff" font-size="24" font-weight="bold">P4 程序数据流</text>
  
  <!-- 报文进入 -->
  <rect x="50" y="100" width="120" height="60" rx="8" fill="#2d2d4a" stroke="#4a4a6a"/>
  <text x="110" y="135" text-anchor="middle" fill="#888888" font-size="14">报文进入</text>
  
  <!-- 箭头 -->
  <polygon points="180,130 200,120 200,140" fill="#4ecdc4"/>
  
  <!-- Parser -->
  <rect x="210" y="80" width="150" height="100" rx="8" fill="#1b4965" stroke="#2d6a4f" stroke-width="2"/>
  <text x="285" y="115" text-anchor="middle" fill="#4ecdc4" font-size="16" font-weight="bold">Parser</text>
  <text x="285" y="140" text-anchor="middle" fill="#3d8b37" font-size="12">解析报文</text>
  <text x="285" y="160" text-anchor="middle" fill="#3d8b37" font-size="12">提取报头</text>
  
  <!-- 箭头 -->
  <polygon points="370,130 390,120 390,140" fill="#4ecdc4"/>
  
  <!-- Control -->
  <rect x="400" y="80" width="150" height="100" rx="8" fill="#1b4965" stroke="#2d6a4f" stroke-width="2"/>
  <text x="475" y="115" text-anchor="middle" fill="#4ecdc4" font-size="16" font-weight="bold">Control</text>
  <text x="475" y="140" text-anchor="middle" fill="#3d8b37" font-size="12">匹配-动作</text>
  <text x="475" y="160" text-anchor="middle" fill="#3d8b37" font-size="12">处理逻辑</text>
  
  <!-- 箭头 -->
  <polygon points="560,130 580,120 580,140" fill="#4ecdc4"/>
  
  <!-- Deparser -->
  <rect x="590" y="80" width="150" height="100" rx="8" fill="#1b4965" stroke="#2d6a4f" stroke-width="2"/>
  <text x="665" y="115" text-anchor="middle" fill="#4ecdc4" font-size="16" font-weight="bold">Deparser</text>
  <text x="665" y="140" text-anchor="middle" fill="#3d8b37" font-size="12">重新打包</text>
  <text x="665" y="160" text-anchor="middle" fill="#3d8b37" font-size="12">输出字节流</text>
  
  <!-- 箭头 -->
  <polygon points="750,130 770,120 770,140" fill="#4ecdc4"/>
  
  <!-- 报文离开 -->
  <rect x="780" y="100" width="120" height="60" rx="8" fill="#2d2d4a" stroke="#4a4a6a"/>
  <text x="840" y="135" text-anchor="middle" fill="#888888" font-size="14">报文离开</text>
  
  <!-- 下方：V1Model 架构 -->
  <text x="500" y="250" text-anchor="middle" fill="#ffffff" font-size="20" font-weight="bold">V1Model 架构（六大块）</text>
  
  <!-- 六个块 -->
  <rect x="50" y="280" width="120" height="60" rx="6" fill="#1b4965"/>
  <text x="110" y="315" text-anchor="middle" fill="#4ecdc4" font-size="12">Parser</text>
  
  <rect x="190" y="280" width="120" height="60" rx="6" fill="#2d6a4f"/>
  <text x="250" y="315" text-anchor="middle" fill="#4ecdc4" font-size="12">VerifyChecksum</text>
  
  <rect x="330" y="280" width="120" height="60" rx="6" fill="#3d8b37"/>
  <text x="390" y="315" text-anchor="middle" fill="#ffffff" font-size="12">Ingress</text>
  
  <rect x="470" y="280" width="120" height="60" rx="6" fill="#3d8b37"/>
  <text x="530" y="315" text-anchor="middle" fill="#ffffff" font-size="12">Egress</text>
  
  <rect x="610" y="280" width="120" height="60" rx="6" fill="#2d6a4f"/>
  <text x="670" y="315" text-anchor="middle" fill="#4ecdc4" font-size="12">ComputeChecksum</text>
  
  <rect x="750" y="280" width="120" height="60" rx="6" fill="#1b4965"/>
  <text x="810" y="315" text-anchor="middle" fill="#4ecdc4" font-size="12">Deparser</text>
  
  <!-- 连接箭头 -->
  <polygon points="175,310 185,305 185,315" fill="#4ecdc4"/>
  <polygon points="315,310 325,305 325,315" fill="#4ecdc4"/>
  <polygon points="455,310 465,305 465,315" fill="#4ecdc4"/>
  <polygon points="595,310 605,305 605,315" fill="#4ecdc4"/>
  <polygon points="735,310 745,305 745,315" fill="#4ecdc4"/>
</svg>
</div>

```
报文进入
   ↓
┌─────────┐
│ Parser  │  ← 解析：从字节流提取报头
└────┬────┘
     ↓
┌─────────────┐
│ Control     │  ← 处理：匹配-动作
│ (Ingress)   │
└────┬────────┘
     ↓
┌─────────────┐
│ Egress      │  ← 出口处理
└────┬────────┘
     ↓
┌──────────┐
│ Deparser │  ← 打包：把报头重新变成字节流
└────┬─────┘
     ↓
报文离开
```

---

## 二、四大组成部分

一个完整的 P4 程序包含四部分：

### 2.1 Header（报头定义）

定义报文的结构，类似 C 语言的结构体：

```p4
header Ethernet_h {
    bit<48> dstAddr;    // 目的 MAC
    bit<48> srcAddr;    // 源 MAC
    bit<16> etherType;  // 类型
}
```

### 2.2 Parser（解析器）

把原始字节流"翻译"成结构化的报头：

```p4
parser MyParser(packet_in pkt, out Headers hdr) {
    state start {
        pkt.extract(hdr.ethernet);  // 提取以太网头
        transition accept;          // 解析完成
    }
}
```

### 2.3 Control（控制逻辑）

定义报文的处理逻辑，核心是 Match-Action 表：

```p4
control MyIngress(inout Headers hdr, inout standard_metadata_t meta) {
    action set_port(bit<9> port) {
        meta.egress_spec = port;  // 设置出端口
    }
    
    table forward {
        key = { hdr.ethernet.dstAddr: exact; }
        actions = { set_port; }
    }
    
    apply {
        forward.apply();
    }
}
```

### 2.4 Deparser（反解析器）

把处理后的报头重新打包成字节流：

```p4
control MyDeparser(packet_out pkt, in Headers hdr) {
    apply {
        pkt.emit(hdr.ethernet);  // 输出以太网头
        pkt.emit(hdr.ipv4);      // 输出 IPv4 头
    }
}
```

---

## 三、完整的 P4 程序长什么样

把四部分组合起来：

```p4
#include <core.p4>
#include <v1model.p4>

// 1. Header 定义
header Ethernet_h {
    bit<48> dstAddr;
    bit<48> srcAddr;
    bit<16> etherType;
}

// 2. Headers 结构
struct Headers {
    Ethernet_h ethernet;
}

// 3. Parser
parser MyParser(packet_in pkt, out Headers hdr) {
    state start {
        pkt.extract(hdr.ethernet);
        transition accept;
    }
}

// 4. Control
control MyIngress(inout Headers hdr, inout standard_metadata_t meta) {
    apply { }
}

// 5. Deparser
control MyDeparser(packet_out pkt, in Headers hdr) {
    apply { pkt.emit(hdr.ethernet); }
}

// 6. 主函数
V1Switch(MyParser(), MyIngress(), MyDeparser()) main;
```

---

## 四、数据流向总结

```
原始字节流
    ↓
  Parser（提取报头）
    ↓
  Control（匹配-动作处理）
    ↓
  Deparser（重新打包）
    ↓
输出字节流
```

每个阶段的输入输出：

| 阶段 | 输入 | 输出 |
|------|------|------|
| Parser | packet_in（字节流） | Headers（结构化报头） |
| Control | Headers + standard_metadata | 修改后的 Headers |
| Deparser | Headers | packet_out（字节流） |

---

## 五、关键概念

| 概念 | 说明 |
|------|------|
| `packet_in` | 输入报文（字节流） |
| `packet_out` | 输出报文（字节流） |
| `Headers` | 所有报头的集合 |
| `standard_metadata` | 标准元数据（端口号、长度等） |
| `V1Switch` | BMv2 的参考架构 |

---

**下一篇**：[Header：网络报文的"身份证"](/blog/p4/p4-03-header) — 深入理解 header 类型

---

## 参考文献

1. P4 Language Consortium. *P4_16 Language Specification Version 1.2.5*
2. 张恒华. *P4 中文教程* 第 02-03 章. https://github.com/zhh2001/p4-language-guide-zh
3. P4.org. *P4 Language*. https://p4.org/
