---
title: "P4 Parser 深入：状态机与报文解析"
publishDate: 2026-07-21 00:00:00
description: "P4 Parser 状态机工作原理、select 语句、extract/verify/lookahead 详解"
tags: ["P4", "Parser", "状态机"]
categories: ["P4"]
language: "中文"
draft: false
comment: true
---

Parser 是 P4 程序的入口，它负责把一串原始字节"解析"成结构化的报头。上一篇我们定义了 header 类型，这一篇来深入理解 Parser 是怎么工作的。

> 本文基于 [P4 中文教程](https://github.com/zhh2001/p4-language-guide-zh) 第 06 章整理，并参考 [P4-16 语言规范](https://p4.org/p4-specs/p4-16/) 相关章节。

---

## 一、Parser 本质是什么

Parser 本质上是一个**有限状态机（FSM）**。每个状态做两件事：

1. 从报文中 `extract`（提取）若干字节到对应的 header
2. 用 `select` 语句根据某个字段的值，决定跳转到下一个状态

整个过程从 `start` 状态开始，到某个 `accept` 状态结束，最终得到一组已经填充好的 header。

---

## 二、select 语句

`select` 是 Parser 的"分支开关"：

```p4
transition select(hdr.ethernet.etherType) {
    0x0800: parse_ipv4;
    0x0806: parse_arp;
    0x8100: parse_vlan;
    default: accept;
}
```

---

## 三、extract：提取报头

`extract` 把报文中的字节"切"出来：

```p4
pkt.extract(hdr.ethernet);
pkt.extract(hdr.ipv4);
```

---

## 四、verify：校验

`verify` 在 Parser 中做条件检查：

```p4
verify(hdr.ipv4.version == 4, error.Ipv4IncorrectVersion);
```

---

## 五、lookahead：偷看但不消费

`lookahead` 可以"偷看"报文中的下一个字段：

```p4
bit<16> nextType = lookahead(bit<16>);
```

---

## 六、小结

| 概念 | 作用 |
|------|------|
| `state` | 状态机的一个节点 |
| `extract` | 从报文中提取字节到 header |
| `select` | 根据字段值选择下一个状态 |
| `verify` | 条件校验 |
| `lookahead` | 偷看下一个字段 |

---

**下一篇**：[P4 控制块与动作](/blog/p4/p4-05-control-block)
